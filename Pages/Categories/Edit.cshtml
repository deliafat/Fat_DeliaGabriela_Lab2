@page
@model Fat_DeliaGabriela_Lab2.Pages.Categories.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>BookCategory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BookCategory.Id" />
            <div class="form-group">
                <label asp-for="BookCategory.BookId" class="control-label"></label>
                <select asp-for="BookCategory.BookId" class="form-control" asp-items="ViewBag.BookId"></select>
                <span asp-validation-for="BookCategory.BookId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BookCategory.CategoryId" class="control-label"></label>
                <select asp-for="BookCategory.CategoryId" class="form-control" asp-items="ViewBag.CategoryId"></select>
                <span asp-validation-for="BookCategory.CategoryId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
